export default function LoginPage() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Login</h1>
      <p>This page is now live. Next we’ll wire it to your existing auth UI.</p>
    </div>
  );
}
